apiVersion: v1
kind: Service
metadata:
  name: {{ include "gcn-service-two.fullname" . }}
  labels:
    {{- include "gcn-service-two.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gcn-service-two.selectorLabels" . | nindent 4 }}
