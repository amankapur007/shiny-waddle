apiVersion: v1
kind: Service
metadata:
  name: {{ include "gcn-services.fullname" . }}
  labels:
    {{- include "gcn-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gcn-services.selectorLabels" . | nindent 4 }}
