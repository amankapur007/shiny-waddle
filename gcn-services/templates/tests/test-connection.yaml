apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcn-services.fullname" . }}-test-connection"
  labels:
    {{- include "gcn-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gcn-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
